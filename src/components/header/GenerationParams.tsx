export default function GenerationParams() {
  return (
    <div className="flex items-center gap-2">
      {/* TODO: implement model, size, steps, sampler, scale controls */}
      <span className="text-xs text-muted-foreground">Params</span>
    </div>
  );
}
